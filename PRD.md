## Problem Statement

The Good Fight browser tracker has its foundation built (Setup, Personnel, Recruitment, Vandalism, Gather Supplies, Scout-start) with 118 passing tests, but the game isn't playable end-to-end yet: there's no Recruit Attempt implementation, no Turn lifecycle, no Crackdown, no Mid/Late-Game Operations, no Victory, and no headless simulation layer. Development so far also produced a known bug (Leader Skill Level drops when it shouldn't) and a testing gap (no test verifies a UI button actually reaches its engine call).

**Playtest Round 1** (first full solo playthrough of the completed engine, `qa-feedback-1.md`) surfaced further issues once the game was actually playable end-to-end: a page-layout bug where the Turn Log grows the page instead of scrolling internally; a Heat-cooldown rule (User Story 8 below) that was mis-transcribed from the rulebook into this PRD, making Heat "reset" nearly every turn instead of accumulating as a real risk meter; a starvation bug in three of the four AI strategies that left them permanently unable to grow past the Leader, which in turn meant simulated batches never reach Victory and always hit the turn-count safety cap; a scatter-chart axis in the simulation dashboard that plots the game index but is easily misread as a turn number; and several genuine gaps in player-facing clarity (no visibility into what an Operation requires or does, no indication of why a locked Operation is locked, no roll-vs-threshold context in the turn log, no sense of proximity to Heat/Influence's meaningful thresholds).

## Solution

Complete the core game engine and UI wiring so a full game (setup → recruit → operate → crackdown → victory) is playable, fix the Leader Skill bug, backfill DOM-wiring tests per ADR-0002, then build the headless simulation layer (reusing the same engine modules, per `plan/simulation.md`'s original architecture) so batches of AI-driven playthroughs can run for balance analysis.

**Playtest Round 1 fixes**: correct the Heat-cooldown rule and the CSS layout bug behind it; add an anti-starvation mechanism to the AI strategies; fix the dashboard heatmap's axis labeling; and add the player-facing clarity features (Operation tooltips with locked-reason messaging, Heat/Influence progress bars, enriched roll-vs-threshold log lines, and batch-running K=1 Operations across multiple Operatives).

Domain vocabulary throughout follows `CONTEXT.md`. Rule interactions follow the source rulebook (`The Good Fight Mini-TTRPG (Solo).pdf`) plus the clarifications below, which resolve places where the rulebook was ambiguous or where `plan.md`/the current code diverged from it.

## User Stories

1. As a player, I want my Leader Skill Level to never decrease once raised, so that losing my best Operative doesn't erase my Leader's progress (fixes the current `Math.max`-mirror bug in `updateLeaderSkill`).
2. As a player, I want every button that triggers a game action to reliably reach its engine logic, so that a wiring mistake can't silently do nothing (per ADR-0002 — DOM-wiring test for every engine-calling button, including the ones already shipped).
3. As a player, I want the in-flight Scout bug fixed, so that starting a Scout operation correctly removes my assigned Operatives from the available pool until it resolves.
4. As a player, I want to attempt Recruitment with the correct dice math, so that my base d10 (or d12 if I spend a Supply) plus my Influence-tier bonus die is summed and compared against the Recruit's card value.
5. As a player, I want to end my turn and have initiate timers advance, detained Operatives return on schedule, and multi-turn Operations advance or resolve, so that the turn lifecycle matches the rulebook.
6. As a player, I want a Crackdown check every end of turn (d100 ≤ Heat), with the correct 5-tier penalty table and cascade substitution (Operative → Initiate → Supplies, each missing unit converting to 2 of the next type down), so that Crackdown consequences are correctly enforced.
7. As a player, I want Operatives and Initiates lost to a Crackdown to be shuffled back into the Recruitment Deck, so the population pool stays consistent.
8. As a player, I want Heat to only cool when a Crackdown actually triggers (reduced by that turn's roll value) or via specific Operation effects, so Heat behaves as a real accumulating risk meter rather than resetting to near-zero almost every turn. (Corrected in Playtest Round 1 — this PRD previously mis-stated the rule as "every turn regardless of trigger"; that was this document's error, not a rulebook deviation.)
9. As a player, I want to run a Late-Game Scout (3-turn Multi-turn Operation, harsher failure penalties than Scout) so that I can uncover Late-Game Operation opportunities.
10. As a player, I want to choose a Difficulty (easy/medium/hard) once at game setup, so the Influence threshold for all my Mid-Game (30/45/60) and Late-Game (60/90/120) Operation attempts for that game is fixed accordingly.
11. As a player, I want to execute an available Mid-Game Operation and roll on the d6 outcome table for its effect, so that scouted opportunities pay off.
12. As a player, I want a failed Mid-Game or Late-Game Operation to capture (not detain) the assigned Operative(s) involved, shuffling their cards back into the Recruitment Deck, matching the rulebook's Operation-tier failure consequence.
13. As a player, I want to execute an available Late-Game Operation and roll on the d8 outcome table, so that completing 3 distinct Late-Game Operations triggers Victory.
14. As a player, I want re-rolling a duplicate Late-Game Operation type on the outcome table to also exclude types I currently have available-but-unexecuted (not just already-completed types), so I never hold two opportunities of the same type at once — a deliberate deviation from the rulebook's literal wording (which only excludes completed types).
15. As a player, I want a Victory screen when I complete 3 Late-Game Operations, summarizing turns taken, operatives lost, and peak Influence.
16. As a player, I want a non-blocking Unwinnable Advisory when I have no Operatives, no Recruit Pool, and an empty Recruitment Deck, so I know my position is likely stuck without being forced to quit.
17. As a player, I want to save/load multiple game slots and see a scrollable turn history log, so I can pick up a game later and review what happened.
18. As a developer, I want a headless simulator that runs one full game using the real engine modules (no DOM), accepting an AI strategy function and a Difficulty setting, so simulation never drifts from the playable game's rules.
19. As a developer, I want Cautious/Aggressive/Balanced/Random AI strategies, each with a hard-coded rule for the Compound Failure choice (detain vs. lose Supplies) reflecting that strategy's personality (e.g. Cautious protects headcount and picks −Supplies; Aggressive protects Supplies and picks detain; Balanced picks whichever resource it currently has more slack in; Random picks randomly).
20. As a developer, I want to run a batch of N games for one Strategy + Difficulty pair and get an aggregate summary (win rate, turn-count stats, resource stats, crackdown stats), so I can compare strategies and difficulties against each other.
21. As a developer, I want per-turn and per-game metrics collected during simulation, so a single game can be drilled into turn-by-turn.
22. As a developer, I want a dashboard (`simulate.html`) with the chart set from `plan/simulation.md` (win rate, turn-count histogram, resource curves, crackdown analysis, operative lifecycle funnel, operation completion heatmap, milestone timeline, single-game drilldown) so results are explorable without reading raw JSON.
23. As a developer, I want CSV/JSON export of simulation results, so I can analyze runs outside the browser.

### Playtest Round 1

24. As a player, I want the Turn Log to scroll within its own panel instead of growing the page, so the End Turn button and every other panel stay in a fixed, fully-visible position no matter how long the game runs.
25. As a player, I want to hover any Operation (including each of the 12 individual Mid-Game/Late-Game Operation types) and see its requirements, success effect, and failure consequence, so I don't have to memorize or look up the rulebook mid-game.
26. As a player, I want every Operation to always be visible — grayed out and disabled when I don't currently meet its requirements, with its tooltip additionally stating exactly what's missing (e.g. "Locked: needs 45 Influence (have 30)") — so I always understand why an option isn't available instead of it silently not appearing.
27. As a player, I want to select multiple untapped units at once for Minor Vandalism or Gather Supplies (the two 1-Operative Operations) and have the engine run the Operation once per selected unit, each an independent roll and outcome, so I don't have to click through single-unit assignments one at a time.
28. As a player, I want every roll-under (d100) check's log line to show what I rolled, the threshold I needed to roll under, and the formula behind that threshold, so I understand why a roll succeeded or failed instead of just seeing a bare number.
29. As a player, I want Heat and Influence shown as progress bars toward their maximums (100 and 500 respectively) in the top status bar, so I have an at-a-glance sense of how close I am to critical Heat or to unlocking Influence-gated Operations.
30. As a developer, I want the fixed AI strategies (Cautious, Balanced, Aggressive) to occasionally deviate from their priority ordering (5%/10%/15% of action choices respectively, sampled uniformly from the full legal action set), so no strategy can get permanently stuck repeating the same action forever when its ordering happens to starve its own bootstrap path (e.g. ranking Gather Supplies above the only Recruit-Pool-seeding action available with just the Leader).
31. As a developer, I want simulated batches to reach Victory or genuine Stall well before the turn-count safety cap, so the fixed strategies' win rates and turn-count stats are meaningful rather than uniformly hitting `DEFAULT_MAX_TURNS`.
32. As a developer, I want the Operation Completion Heatmap's Y axis clearly labeled as the game index within the batch (not a turn count) and each point's actual turn color-encoded on a sequential scale, so the chart can't be misread as showing milestones completed on turn 0, 1, 2, ...

## Implementation Decisions

- **Leader Skill Level is a ratchet.** It only ever increases to match the highest card value any current or past Operative has reached; it must never be recomputed as a live `Math.max` over currently-available Operatives. Confirmed by the source PDF's Turns section ("your skill doesn't go down if Operative is lost").
- **Recruit Attempt roll = base die + bonus die, summed.** Base die is d10, or d12 if the player spends 1 Supply. Bonus die is 0 (no die) below 50 Influence, then d4 at 50, incrementing (d6/d8/d10/d12/d20) every 50 Influence thereafter, capped at d20. Sum compared to the Recruit's card value; roll ≥ value succeeds.
- **Difficulty is a new game-wide setting**, chosen once at the Setup screen alongside Resistance Values/Regime Type/Input Mode (unlike those, Difficulty has real mechanical effect — it is not flavor). It fixes which Influence threshold (30/45/60 for Mid-Game, 60/90/120 for Late-Game) gates Operation attempts for the rest of that game.
- **Crackdown Cascade is a chained multiplier**, not a flat fallback: for a penalty requiring N of personnel-type A, each of the N not available converts to 2 units of the next type down (Operative → Initiate → Supplies), and this conversion can chain a second time (missing Initiates → Supplies) if the intermediate type is also unavailable. Worked example: Warehouse raid (roll 61–80) with 1 Operative and 0 Initiates available → −1 Operative (all that's available) + the 1 missing Operative converts to 2 Initiates owed → 0 Initiates available converts that to 2×2 = −4 Supplies. Net: −1 Operative, −4 Supplies.
- **Compound Failure's second penalty is a genuine free player choice** (detain vs. lose Supplies), not availability-gated — this was previously fixed correctly (see `questions.md`) and stays as-is; do not rework `resolveSignificantVandalism`/`resolveScout`/etc.
- **Mid-Game/Late-Game Operation failure captures, not detains.** Per the PDF's Operations table, failure captures (permanently loses, card recycled to the Recruitment Deck) the assigned Operative(s) — this is a different consequence than the Detained-Operative penalty used by Vandalism/Scout failures. Do not reuse the Detained code path for these.
- **Late-Game Operations table is rolled with a d6, not the PDF's printed "d8"** — confirmed misprint: the table only defines 6 rows (matching the Mid-Game Operations table, correctly labeled d6 for its own 6 rows), and the rulebook has no rows 7–8. When rolling this table (on a successful Late-Game Scout), re-roll if the resulting type is already completed *or* already sitting unexecuted in `availableLateGameOps`. This dedup rule is broader than the PDF's literal "already been successfully executed" wording — a deliberate deviation, worth a one-line comment at the reroll site pointing back to this decision.
- **Testing seam**, per ADR-0001/ADR-0002: engine modules (`state`/`deck`/`dice`/`operations`/`crackdown`/`turn`) are tested as pure async functions with injected Dice/Deck providers (existing pattern in `test-operations.js`). Every button/handler that calls into an engine module additionally gets a DOM-wiring test (happy-dom, existing pattern in `test-app.js`/`test-ui.js`) asserting the click reaches that call — including buttons already shipped in Phases 1–2, which currently lack this coverage.
- **Simulation reuses the engine modules with zero parallel implementation** (per `plan/simulation.md`) — `simulator.js`/`strategies.js`/`metrics.js`/`batch.js` under `js/simulation/`, with Input Mode always forced to digital.
- **Difficulty is a batch-level simulation parameter**, selectable like Strategy — a batch runs N games at one Strategy + Difficulty pair; comparing difficulties means running separate batches and overlaying results, the same pattern already used for comparing strategies.
- **Turn lifecycle order**: advance initiate timers → release expired Detained Operatives → advance Multi-turn Operations (resolving any that complete) → Crackdown check (roll d100 ≤ Heat) → **if triggered**, apply the tier penalty and reduce Heat by the roll's value → update Leader Skill (ratchet) → increment turn counter. (Corrected in Playtest Round 1 — see the Heat cooldown decision below; the Heat reduction step is no longer unconditional.)
- **Heat cooldown is trigger-gated, not unconditional** (Playtest Round 1 correction of User Story 8 / the turn-lifecycle-order decision above, both of which mis-stated this as "every turn regardless of trigger" — this PRD's error, not a rulebook deviation). Heat now only decreases via: (a) a triggered Crackdown, reduced by that turn's roll value, or (b) the existing Mid/Late-Game Operation effects that already reduce Heat (embed_mole, hack_comm_tower, industry_strike, news_agency, establish_militia, neutralize_leadership — unaffected by this change). `Crackdown.resolveCrackdown` moves its `GameState.setHeat` call inside the `triggered` branch.
- **Operation availability rendering changes from hide-when-unavailable to always-visible-and-grayed-out.** Every Operation button always renders; `Operations.canExecute` (and the Mid/Late-Game equivalents) now drive a disabled/grayed visual state plus the locked-reason tooltip line, rather than gating whether the button exists in the DOM at all.
- **Operation tooltip content is static per operation type**, sourced from the same data `operations.js` already encodes (`OPERATION_REQS`, and the Mid-Game/Late-Game per-type success effects in `applyMidGameEffect`/`applyLateGameEffect`) — no live/computed success-probability display. One unified tooltip per operation: base content (requirements, success effect, failure consequence) is always shown; a "Locked: needs X (have Y)" line is appended only when the operation is currently unavailable, covering whichever resource(s) — Influence, Supplies, or Operatives — are short.
- **K=1 Operation batch-running is scoped to Minor Vandalism and Gather Supplies only** (the only two Operations with a 1-Operative requirement). Selecting N untapped units runs N fully independent resolutions (own roll, own consequences each), not a single batched roll. Not extended to fixed K>1 Operations (Average/Significant Vandalism, Scout, Late-Game Scout, Mid/Late-Game Ops) — those keep their current single-assignment flow.
- **Roll-vs-threshold log enrichment is a single shared formatting seam**, not duplicated per operation call site: one pure helper builds the "rolled N (need ≤ T: formula breakdown)" string (with the roll value and the threshold each wrapped for distinct styling) for every d100-check-based Operation (Vandalism tiers, Gather Supplies per-roll, Scout, Late-Game Scout, Mid/Late-Game Ops). Recruit's existing breakdown-style log line (`"(d10: 7 + d4: 3 = 10 vs 8)"`) is the prior art this generalizes from, made explicit about the roll-under (≤) direction.
- **Heat and Influence progress bars use the existing hard caps already enforced by `GameState`**: Heat 0–100, Influence 0–500 (both already clamped by `setHeat`/`setInfluence`). Supplies has no fixed ceiling and gets no progress bar.
- **AI strategy anti-starvation is epsilon-greedy exploration, not a hand-tuned bootstrap rule or a repetition-streak counter.** Each time a fixed strategy (Cautious/Balanced/Aggressive) chooses an action, with probability epsilon it samples uniformly from the full legal action set (`Strategies.legalActions`) instead of following its priority ordering; otherwise it follows the ordering as today. Epsilon: Cautious 0.05, Balanced 0.10, Aggressive 0.15 — chosen as a gradient matching each personality's risk profile. Reuses the existing injectable RNG (`Strategies.setRandomSource`) so tests stay deterministic. The Random strategy is unaffected (it already samples uniformly).
- **Operation Completion Heatmap keeps its existing game×stage scatter-grid shape** (Playtest Round 1: only the labeling/encoding was wrong, not the underlying chart design). The Y axis gets an explicit "Game #" label so it can't be mistaken for a turn count, and each point is additionally color-encoded by its actual `turn` value on a sequential scale (in addition to the existing tooltip). No change to the click-to-drilldown behavior.

## Testing Decisions

- Tests verify behavior through public interfaces, never internal state shape or private helpers — a test should keep passing across a refactor that doesn't change observable behavior.
- Tracer-bullet TDD throughout (ADR-0001): one seam confirmed, one failing test written, minimal implementation, green, repeat. No batch-writing a module's full test suite before any implementation exists.
- Engine-level tests: prior art is `test-operations.js` — inject known Dice/Deck results via `Dice.setProvider`/`Deck.setProvider`, call the engine function, assert on the returned/mutated state.
- DOM-wiring tests: prior art is `test-app.js`'s screen-router tests and `test-ui.js`'s modal tests — construct the DOM via happy-dom, dispatch the click, assert the expected engine function was invoked (or its observable effect occurred).
- Simulation correctness is engine-level too: `simulator.js` calling the same `operations.js`/`crackdown.js`/`turn.js` functions as the playable game means no separate rule-correctness tests are needed for simulation — only strategy-decision and metrics-aggregation logic need their own tests.

### Playtest Round 1

- **Heat cooldown correction**: extend `test-crackdown.js` (existing engine seam) — assert Heat is unchanged on a non-triggering turn and reduced by the roll's value only on a triggering turn.
- **AI strategy anti-starvation**: extend `test-strategies.js` (existing seam, prior art is the Compound-Failure and legal-action tests already there) — using `Strategies.setRandomSource` to force the RNG above/below each strategy's epsilon, assert (a) below epsilon, a legal action outside the priority order's top choice can be returned, and (b) above epsilon, the priority ordering's top legal choice is returned as before.
- **Operation Completion Heatmap fix**: extend `test-dashboard.js` (existing seam, prior art is `operationCompletionHeatmap`'s existing fixture-driven tests) — assert the Y-axis label metadata reads "Game #" and each cell/point carries a color/turn encoding derived from its `turn` value.
- **Roll-vs-threshold log formatting**: new pure-function seam (e.g. a `formatCheckResult`-style helper) unit-tested directly with fixed inputs (roll, heat, modifiers) asserting the exact rendered string and that roll/threshold are wrapped for distinct styling — no DOM needed for this seam, since it's pure string formatting consumed by the existing `addLogEntry` call sites.
- **K=1 batch-running**: DOM-wiring test (existing `test-app.js` pattern, happy-dom) — select N untapped units for Gather Supplies with a fixed `Dice.setProvider` sequence, assert N independent resolutions occurred (N log entries, resource deltas matching N separate rolls).
- **Operation tooltip content + locked-reason line**: DOM-wiring test (existing `test-ui.js` modal-pattern seam) — assert each operation button's tooltip text matches its static content, and that the "Locked: needs X (have Y)" line appears only when `canExecute` (or the Mid/Late-Game equivalent) is false, naming the actually-short resource(s).
- **Always-visible grayed-out operations**: DOM-wiring test (existing `test-app.js` screen-router pattern) — assert every operation button is present in the DOM regardless of `canExecute`, with a disabled/grayed class applied exactly when unavailable (replacing the current tests that assert a button is absent when illegal, per the hide → gray-out behavior change).
- **Turn Log page-layout fix (CSS `height` vs `min-height`)** has no automated seam — happy-dom doesn't compute real layout — so this is manual/visual verification only: confirm the Turn Log scrolls within its own panel and the End Turn button stays fixed regardless of log length, across the real browser.
- **Heat/Influence progress bars**: DOM-wiring test (existing `test-app.js`/render pattern) — assert the rendered progress-bar width/fill-percentage matches `state.heat/100` and `state.influence/500` for a few representative state values.

## Out of Scope

- Dice-roll and Crackdown-roll UI animation polish (cosmetic, `plan.md` Phase 6 item, not gameplay-blocking).
- Undo-last-action.
- Keyboard shortcuts.
- Mobile-responsive layout (desktop/tablet is the target).
- Puppeteer/Playwright migration (only needed if real-browser rendering tests become necessary later).

### Playtest Round 1

- Live/computed success-probability display in Operation tooltips (static rule text only).
- Batch-running for fixed K>1 Operations (Average/Significant Vandalism, Scout, Late-Game Scout, Mid/Late-Game Ops) — batch-running is scoped to the two 1-Operative Operations only.
- Turn-log entry reversal (newest-on-top) — superseded during grilling by the panel-scroll fix; the log stays newest-at-bottom with auto-scroll-to-bottom.
- A repetition-streak-based anti-starvation mechanism (tracking consecutive same-action counts and excluding after a threshold) — epsilon-greedy exploration was chosen instead as the more general, less-stateful mechanism.
- Redesigning the Operation Completion Heatmap as a true turn-bucket matrix (X = turn range, color = game count) — the existing game×stage scatter-grid shape is kept; only its axis labeling and turn color-encoding change.
- Influence progress-bar milestone tick marks for Mid-Game/Late-Game thresholds — superseded by the always-visible grayed-out-Operations-with-tooltip approach, which covers the same "why can't I do this yet" need for every resource type, not just Influence.
- Supplies progress bar — Supplies has no fixed ceiling (unlike Heat's 100 and Influence's 500 caps), so a proximity-to-max bar doesn't apply.

## Further Notes

`plan/` (plan.md, questions.md, simulation.md, test-driven-development.md, 2026-02-23-dev-log.md) is being mined into this PRD, `CONTEXT.md`, and `docs/adr/`, then deleted — this PRD plus the to-tickets breakdown that follows it are the new source of truth for remaining work, replacing the phase-based plan.

The Playtest Round 1 stories/decisions above were mined from `qa-feedback-1.md` (the first full solo playthrough of the completed engine) via a `/grill-with-docs` session; that file's observations are now fully captured here and in the tickets filed from this PRD.
